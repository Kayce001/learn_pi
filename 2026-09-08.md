# 2026-09-08

今天大部分时间都在学习 Pi。

先粗略看了一遍 `pi/packages/agent/docs` 目录下的各个文档，主要是了解这些文档分别在讲什么、各自解决什么问题。之后开始重点学习 `harness.md` 的 Part 3，也就是 **Operation**。

我目前对 Operation 的理解是：它有点像在 Codex 里输入一个 prompt，然后 Codex 开始执行这一整个任务。

而 Harness 的核心价值，就是让这个任务能够**可靠地持续推进**，即使中途发生进程崩溃、任务中断，也能够根据之前留下的状态恢复，而不是完全从头开始。

因此，Operation 里面会有很多记录点，需要把整个执行过程拆成一系列事件，并把关键状态详细记录下来。

比如到了某个 checkpoint，系统已经决定下一步要调用模型。这时候并不是直接调用模型，而是先把“准备进行这次模型调用”这件事登记下来，确认记录已经落盘之后，再真正启动模型调用。

我觉得这也是 Harness 和普通 Agent 一个很重要的区别。

Harness 会持续把任务执行过程中的关键状态写入持久化存储，也就是在硬盘上留下记录；而普通 Agent 的很多状态主要存在于当前进程和内存中。

所以如果普通 Agent 的进程突然中断，它很可能不知道之前执行到了哪一步；而 Harness 因为保留了这些记录，就有机会在恢复之后继续判断：之前已经完成了什么、正在做什么、下一步应该从哪里继续推进。
