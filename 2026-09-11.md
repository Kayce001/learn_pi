# 2026-09-11

今天用了一天时间，做完了第一个 Pi Extension 插件，算是对 Pi 又多了一分了解。

我的理解是，Extension 比 Tool 的范围更广。除了工具之外，还可以通过 Hooks 在 Harness 中插入逻辑，改变 Agent 的行为。

今天主要学习了 **Public Surface**，也就是 Pi 对外提供的接口，比如 `prompt()`，以及 Tools 的注册接口、Hook 接口等。

外层接口和具体实现是分开的，上层只需要调用接口就好了。
