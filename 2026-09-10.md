# 2026-09-10

今天根据一篇文章了解了 GPT-6 的模型架构，对其中 **Loop** 这个概念感觉很有意思。

我的理解是，通过重复使用同一组模型结构，可以在不大幅增加参数量的情况下增加计算深度。这样一来，未来有可能用更小的显存运行更强的模型，甚至在具身智能场景中，把比较强的模型直接部署到机器人本地。

然后继续学习了 Pi 相关内容。

今天主要了解了 Pi Harness 的 **Public Surface 和内部实现之间的区别**。

Public Surface 可以理解成 Harness 对外暴露出来、让开发者直接使用的那一层接口。比如我们可以通过这些接口去创建 Lane、给 Lane 发任务、设置模型、设置 thinking level、选择启用哪些工具等。

而这些接口背后，Harness 内部还要负责很多更复杂的事情，比如 Operation 怎么运行、状态怎么记录、任务中断后怎么恢复、工具调用怎么执行等。

另外我也进一步理解了 Harness 和 Lane 的关系：Harness 可以注册很多模型和工具，但具体某个 Lane 使用哪些工具、使用哪个模型，是由 Lane 自己决定的。

也就是说，Harness 更像是提供一个统一的能力池，而每个 Lane 根据自己的任务选择实际需要的模型和工具。

**Harness 负责提供能力和底层运行机制，Lane 负责决定自己具体使用哪些能力。**
